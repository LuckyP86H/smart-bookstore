# Test dependencies. The unit tests stub the heavy ML/LLM/DB packages
# (sentence-transformers/torch, litellm, psycopg2), so CI only needs the web
# framework plus the test runner. Shared packages are pinned to the same
# versions as requirements.txt — keep them in sync when bumping there.
# (pip constraint files can't be used here: requirements.txt contains
# extras like uvicorn[standard], which constraints reject.)
fastapi==0.115.6
pydantic==2.10.5
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx>=0.23.0,<0.28.0

pytest==8.3.4
pytest-asyncio==0.25.2
